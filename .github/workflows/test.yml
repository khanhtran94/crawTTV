name: Test Schedule

on:
  workflow_dispatch:
  schedule:
    - cron: "20,40 * * * *"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test scheduled trigger
        run: |
          echo "Event: ${{ github.event_name }}"
          echo "UTC time:"
          date -u