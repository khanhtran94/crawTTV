name: Test Schedule

on:
  workflow_dispatch:
  schedule:
    - cron: "6,16,26,36,46,56 * * * *"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test scheduled trigger
        run: |
          echo "Event: ${{ github.event_name }}"
          echo "UTC time:"
          date -u